package Java8;

import java.util.Arrays;
import java.util.List;
import java.util.stream.Collectors;

public class FilterEx {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		List<String> list = Arrays.asList("hello", "world", null, "world", "temp", "hello", "java", "hi", "stream");
         list.stream().filter(Str->str!=null).collect(Collectors.toList()).forEach(System.out::println);
	}

}
